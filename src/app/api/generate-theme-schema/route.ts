import { NextResponse } from 'next/server';

/**
 * Default theme configuration used as a starting point for new themes.
 * Mirrors the PageTheme shape from builder-types.ts.
 */
const DEFAULT_THEME_CONFIG = {
  name: 'Mi Theme WordPress',
  slug: 'my-theme',
  description: 'A professional WordPress theme generated by PageForge',
  version: '1.0.0',
  author: 'PageForge',
  authorUri: 'https://pageforge.dev',
  license: 'GPL-2.0-or-later',
  licenseUri: 'https://www.gnu.org/licenses/gpl-2.0.html',
  textDomain: 'my-theme',
  tags: 'one-column, custom-menu, featured-images, threaded-comments, translation-ready',
  template: 'landing' as const,
  theme: {
    primaryColor: '#2563EB',
    secondaryColor: '#7C3AED',
    accentColor: '#F59E0B',
    backgroundColor: '#FFFFFF',
    textColor: '#1F2937',
    headingFont: 'Inter',
    bodyFont: 'Inter',
    borderRadius: 'medium' as const,
    style: 'modern' as const,
  },
  sections: [
    { type: 'hero', enabled: true },
    { type: 'features', enabled: true },
    { type: 'about', enabled: true },
    { type: 'testimonials', enabled: true },
    { type: 'cta', enabled: true },
    { type: 'footer', enabled: true },
  ],
};

/**
 * GET /api/generate-theme-schema
 *
 * Returns the ThemeConfig schema so the frontend knows what fields
 * to send when calling POST /api/generate-theme.
 */
export async function GET() {
  return NextResponse.json({
    schema: {
      // --- Theme Identity ---
      name:           { type: 'string',  required: true,  label: 'Nombre del Theme',        description: 'Display name of the theme (shown in WP admin)' },
      slug:           { type: 'string',  required: true,  label: 'URL Amigable del Theme',         description: 'Machine-readable identifier (lowercase, hyphens)' },
      description:    { type: 'string',  required: false, label: 'Descripción',        description: 'Brief description of the theme' },
      version:        { type: 'string',  required: false, label: 'Versión',            description: 'Semantic version', default: '1.0.0' },
      author:         { type: 'string',  required: false, label: 'Autor',             description: 'Theme author name', default: 'PageForge' },
      authorUri:      { type: 'string',  required: false, label: 'URL del Autor',         description: 'Author website URL' },
      license:        { type: 'string',  required: false, label: 'Licencia',            description: 'License identifier', default: 'GPL-2.0-or-later' },
      licenseUri:     { type: 'string',  required: false, label: 'URL de la Licencia',        description: 'Link to license text' },
      textDomain:     { type: 'string',  required: false, label: 'Dominio de Traducción',        description: 'Translation text domain (defaults to slug)' },
      tags:           { type: 'string',  required: false, label: 'Etiquetas',               description: 'Comma-separated WordPress.org tags' },

      // --- Template ---
      template:       { type: 'string',  required: false, label: 'Tipo de Plantilla',      description: 'Base template for the theme' },

      // --- Theme Styling ---
      theme: {
        type: 'object',
        required: false,
        label: 'Estilo del Theme',
        description: 'Visual style configuration',
        properties: {
          primaryColor:   { type: 'string', required: false, label: 'Color Primario',    default: '#2563EB' },
          secondaryColor: { type: 'string', required: false, label: 'Color Secundario',  default: '#7C3AED' },
          accentColor:    { type: 'string', required: false, label: 'Color de Acento',     default: '#F59E0B' },
          backgroundColor:{ type: 'string', required: false, label: 'Color de Fondo', default: '#FFFFFF' },
          textColor:      { type: 'string', required: false, label: 'Color de Texto',       default: '#1F2937' },
          headingFont:    { type: 'string', required: false, label: 'Fuente de Títulos',     default: 'Inter' },
          bodyFont:       { type: 'string', required: false, label: 'Fuente del Cuerpo',        default: 'Inter' },
          borderRadius:   { type: 'string', required: false, label: 'Radio de Bordes',    default: 'medium', enum: ['none', 'small', 'medium', 'large', 'full'] },
          style:          { type: 'string', required: false, label: 'Estilo de Diseño',     default: 'modern', enum: ['modern', 'classic', 'minimal', 'bold'] },
        },
      },

      // --- Sections ---
      sections: { type: 'array', required: false, label: 'Secciones', description: 'Array of section objects with type and enabled flag' },
    },
    sectionTypes: [
      'hero',
      'about',
      'services',
      'features',
      'testimonials',
      'pricing',
      'cta',
      'contact',
      'gallery',
      'faq',
      'stats',
      'team',
      'blog_posts',
      'navbar',
      'footer',
    ],
    templateTypes: [
      'landing',
      'portfolio',
      'restaurant',
      'saas',
      'agency',
      'ecommerce',
      'blog',
    ],
    defaultConfig: DEFAULT_THEME_CONFIG,
  });
}
